.configure-checkpoint-settings-view {
    flex-grow: 1;
}

.checkpoint-settings-title {
    -unity-font-style: bold;
    margin-bottom: 6px;
    margin-top: 16px;
}

.checkpoint-settings-content {
    padding-left: 4px;
}

.checkpoint-settings-row {
    flex-direction: row;
    align-items: center;
    margin-bottom: 8px;
    min-height: 22px;
}

.checkpoint-settings-label {
    min-width: 160px;
    width: 160px;
}

.checkpoint-settings-value {
    flex-direction: row;
    flex-grow: 1;
    align-items: center;
}

.checkpoint-disclaimer-icon {
    width: 16px;
    height: 16px;
    margin-right: 8px;
    margin-top: 2px;
    flex-shrink: 0;
}

#checkpointDisclaimerText {
    white-space: normal;
    flex-shrink: 1;
    opacity: 0.8;
    font-size: 11px;
}

.checkpoint-toggle-container {
    flex-direction: row;
    align-items: center;
}

.checkpoint-initializing {
    flex-direction: row;
    align-items: center;
    margin-left: 8px;
}

#initializingLabel {
    margin-left: 4px;
    opacity: 0.6;
}

#gitInstanceDropdown {
    flex-grow: 1;
    min-width: 200px;
}

.checkpoint-validation-row {
    margin-left: 160px;
    margin-top: -4px;
    margin-bottom: 8px;
}

#gitValidationLabel {
    opacity: 0.7;
    font-size: 11px;
}

#gitValidationLabel.checkpoint-validation-success {
    color: var(--muse-chat-icon-success-color);
}

#customGitPathField {
    flex-grow: 1;
}

.checkpoint-browse-button {
    margin-left: 4px;
    min-width: 60px;
}

#discoveryBannerRow .checkpoint-browse-button {
    margin-left: 3px;
}

.checkpoint-error-container {
    flex-direction: row;
    align-items: flex-start;
    padding: 8px;
    margin-bottom: 8px;
    background-color: var(--muse-chat-code-mark-error);
    border-radius: 4px;
    border-width: 1px;
    border-color: var(--muse-chat-icon-error-color);
}

.checkpoint-error-icon {
    flex-shrink: 0;
    margin-right: 8px;
}

#gitValidationErrorText {
    white-space: normal;
    flex-shrink: 1;
}

#repositoryLocationField {
    flex-shrink: 1;
}

#repositoryLocationField > TextInput {
    background-color: var(--muse-chat-backgrounds-100);
    opacity: 0.8;
}

.checkpoint-retention-row {
    flex-direction: row;
    align-items: center;
}

#checkpointRetentionWeeksDropdown {
    width: 60px;
}

.checkpoint-retention-suffix {
    margin-left: 6px;
    opacity: 0.8;
}

.checkpoint-verification-section {
    margin-top: 4px;
}

#verificationStatusLabel {
    font-size: 12px;
    margin-bottom: 8px;
}

.checkpoint-verification-status--ok {
    color: var(--muse-chat-icon-success-color);
}

.checkpoint-verification-status--failed {
    color: var(--muse-chat-icon-error-color);
}

.checkpoint-verification-failure-group {
    display: flex;
}

.checkpoint-verification-section.checkpoint-verification-ok .checkpoint-verification-failure-group {
    display: none;
}

.checkpoint-missing-files-header {
    font-size: 12px;
    -unity-font-style: bold;
    margin-bottom: 4px;
}

.checkpoint-missing-files-list {
    max-height: 120px;
    border-width: 1px;
    border-color: var(--muse-chat-backgrounds-50);
    border-radius: 4px;
    margin-bottom: 8px;
    padding: 4px;
}

.checkpoint-missing-files-content {
    white-space: normal;
    font-size: 11px;
}

.checkpoint-missing-files-overflow {
    font-size: 11px;
    opacity: 0.7;
    margin-top: 2px;
}

.checkpoint-verification-actions {
    flex-direction: row;
    align-items: flex-start;
    flex-wrap: wrap;
    margin-bottom: 4px;
}

.checkpoint-verification-button {
    margin-right: 8px;
    margin-bottom: 4px;
}

.checkpoint-initialize-anyway-group {
    flex-direction: column;
}

.checkpoint-initialize-anyway-button {
    margin-right: 0;
}

.checkpoint-initialize-anyway-warning {
    flex-direction: row;
    align-items: flex-start;
    background-color: var(--muse-chat-warning-background-color);
    border-radius: 4px;
    padding: 8px;
    margin-top: 4px;
}

.checkpoint-initialize-anyway-warning--hidden {
    display: none;
}

.checkpoint-initialize-anyway-warning-text {
    white-space: normal;
    font-size: 11px;
    flex-grow: 1;
}
